import React from 'react';

// should only render one < h1 /> inside of the < div />
// should render 'Home Page' inside of the < h1 />

const Home = () => {
  return (
    <div>
      <h1>Home Page</h1>
    </div>
  );
};

export default Home;
